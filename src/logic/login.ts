// Create a login method that takes a email and password and returns a promise jwt.
import fetch from 'node-fetch';

export const login = async (email: string, password: string) => {
    return null;
}